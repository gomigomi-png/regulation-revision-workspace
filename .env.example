# Neon / Vercel が注入する Postgres 接続文字列。
# 値は GitHub に上げない。手元では .env.local に書く。
DATABASE_URL=

# 手元で Neon を使う手順:
# 1. Vercel の Environment Variables から DATABASE_URL をコピー
# 2. .env.local に DATABASE_URL=... を書く
# 3. npm run db:setup で 3 表作成 + 初期データ投入
